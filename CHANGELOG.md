# 更新日志

本文件记录 hhuthesis 各版本的重要变更。

## v3.4.0 - 2026-09-20

### 新增

- 新增 `anonymous` 文类选项，用于生成匿名送审稿。
- 支持省略答辩委员会末尾成员。

### 修复

- 修复国家图书馆封面和英文信息封面生成命令误声明必选参数的问题。
- 修复中文信息封面下划线断裂问题。
- 按照博士学位论文格式要求调整章节标题间距、英文信息封面字体、参考文献字号及双面装订页边距。

## v3.3.0 - 2026-09-15

### 新增

- 支持 `\tutorinfob` 的单位与地址分离设置，并兼容原单参数用法。
- 新增 `\infolinespacing` 命令以调整中文信息封面行距。

### 变更

- 重构中文信息封面排版，支持长字段自适应换行。
- 调整页面边注宽度。

### 修复

- 修复导师信息、论文标题、封面元数据及 logo 的排版对齐问题。
- 修复附录中公式、图、表和代码编号问题。

## v3.2.2 - 2026-05-22

### 新增

- 新增 `newtxopts` 键值型文类选项，将选项透传至 `newtx`；该选项需配合 `tonefont` 使用，未启用 `tonefont` 时静默忽略。

### 变更

- 改用 LaTeX 内核 l3keys 处理 `tonefont` 和 `newtxopts` 选项。
- 移除 `kvoptions` 依赖。

### 修复

- 修复键值选项导致的 `Unused global option` 警告。

## v3.2.1 - 2026-05-22

### 修复

- 为 `ulem` 宏包添加 `normalem` 选项，恢复 `\emph` 的正常斜体行为，并避免与 `algorithm2e` 的 `function` 环境标题冲突。

## v3.2.0 - 2026-05-22

### 新增

- 新增 `tonefont` 文类选项，用于切换至 T1 编码字体模式，并通过 `newtx` 统一加载三族 Times 风格 Type1 字体；中文字体保持不变。

## v3.1.0 - 2026-05-22

### 变更

- 不再默认加载 `bm` 宏包，建议用户在导言区末尾手动添加 `\usepackage{bm}`。
- 多行双语题注改为居中显示，英文题注标签缩写改为 “Fig.”。
- 为 `enumitem` 宏包添加 `inline` 选项以支持行内列表。

## v3.0.0 - 2026-05-07

### 变更

- 参考文献后端由 natbib 和 BibTeX 迁移至 biblatex 和 biber。
- 采用 biblatex-gb7714-2015 样式处理 GB/T 7714 标准格式。
- 移除自定义 `\upcite` 命令，改用 biblatex 原生的 `\cite` 和 `\supercite` 命令。
- 删除 `hhuthesis-numeric.bst` 文件。

## v2.0.2 - 2026-05-07

### 变更

- 将 logo 替换为高分辨率 PDF 版本，并清理废弃图片文件。

## v2.0.1 - 2026-05-07

### 变更

- 将 dtx 源文件与发布版 cls 对齐，确立 dtx 为第一手资料。
- 补回 cls 中已有但 dtx 中遗漏的 `indentfirst` 宏包。
- 补齐 dtx 代码块中缺失的行尾注释符，消除 DocStrip 生成 cls 时宏定义中的多余空格。

### 修复

- 修复因 dtx 与 cls 不同步导致的封面页排版异常。

## v2.0.0 - 2021-04-07

### 新增

- 增加论文中缝（书脊）页。

### 变更

- 按照编写格式要求修改论文国家图书馆封面、中文信息封面、英文信息封面和声明授权页。
- 修改论文页眉：单页页眉改为“河海大学 XX 学位论文”，双页页眉改为所在章节的一级标题名，页眉下划线改为单线。
- 按照编写格式要求修改论文章节标题的字体、字号、对齐方式以及段前、段后距离。
- 修改论文附录格式，去除个人简历。
- 修复参考文献格式并添加文献标识符。

## v1.0.0 - 2020-07-19

- 第一个可用版本。
